---
import * as si from "simple-icons";
import { profile } from "../data/profile";

interface SimpleIcon {
  title: string;
  path: string;
}

// Map a skill label to a simple-icons key. Concepts without a brand logo
// (e.g. "REST APIs") fall back to a neutral monochrome code glyph.
const ICONS: Record<string, string> = {
  Python: "siPython",
  FastAPI: "siFastapi",
  Java: "siOpenjdk",
  "C / C++": "siCplusplus",
  PHP: "siPhp",
  JavaScript: "siJavascript",
  Astro: "siAstro",
  "Tailwind CSS": "siTailwindcss",
  "React Native": "siReact",
  "HTML / CSS": "siHtml5",
  Docker: "siDocker",
  PostgreSQL: "siPostgresql",
  MongoDB: "siMongodb",
  "Git / GitHub": "siGithub",
  Cloudflare: "siCloudflare",
  Linux: "siLinux",
};

function iconFor(label: string): SimpleIcon | null {
  const key = ICONS[label];
  return key ? ((si as Record<string, SimpleIcon>)[key] ?? null) : null;
}
---

<section id="skills" class="py-16 md:py-24">
  <div class="container-x">
    <h2 class="display mb-10 text-4xl sm:text-5xl">
      <span class="block">Tools I</span>
      <span class="ghost block">Work with</span>
    </h2>

    <!-- One cohesive panel, organized in columns, each item with its logo. -->
    <div
      class="grid gap-x-8 gap-y-10 rounded-2xl border border-line bg-surface/40 p-7 sm:grid-cols-2 lg:grid-cols-3 lg:p-9"
    >
      {
        profile.skills.map((group) => (
          <div>
            <h3 class="mb-5 text-sm font-semibold uppercase tracking-wide text-accent">
              {group.category}
            </h3>
            <ul class="space-y-3.5">
              {group.items.map((item) => {
                const icon = iconFor(item);
                return (
                  <li class="group flex items-center gap-3">
                    {icon ? (
                      <svg
                        role="img"
                        viewBox="0 0 24 24"
                        class="h-[18px] w-[18px] shrink-0 fill-muted transition-colors group-hover:fill-ink"
                        xmlns="http://www.w3.org/2000/svg"
                      >
                        <path d={icon.path} />
                      </svg>
                    ) : (
                      <svg
                        viewBox="0 0 24 24"
                        class="h-[18px] w-[18px] shrink-0 fill-none stroke-muted transition-colors group-hover:stroke-ink"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        xmlns="http://www.w3.org/2000/svg"
                      >
                        <path d="M8 9l-3 3 3 3M16 9l3 3-3 3" />
                      </svg>
                    )}
                    <span class="text-sm text-ink/80 transition-colors group-hover:text-ink">
                      {item}
                    </span>
                  </li>
                );
              })}
            </ul>
          </div>
        ))
      }
    </div>
  </div>
</section>
